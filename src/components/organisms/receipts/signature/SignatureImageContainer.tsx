import React from "react";
import SignatureImageLayout from "@/src/components/molecules/signatureModal/signatureImage/SignatureImageLayout";
const SignatureImageContainer = () => {
  return <SignatureImageLayout />;
};

export default SignatureImageContainer;
